spring.application.name=meditrack-backend

# ===============================
# = DATABASE CONFIGURATION (MySQL)
# ===============================

spring.datasource.url=jdbc:mysql://localhost:3306/meditrack?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1234

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update


# ===============================
# = JPA SETTINGS
# ===============================

spring.sql.init.mode=never
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ===============================
# = CONNECTION POOL (HikariCP - default)
# ===============================

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000

# ===============================
# = SERVER
# ===============================

server.port=8080

# ===============================
# = SECURITY CONFIG (TO ENABLE LATER)
# ===============================

# Enable default Spring Security user (for quick testing only)
# spring.security.user.name=admin
# spring.security.user.password=admin123
# spring.security.user.roles=ADMIN

# JWT configuration (if you implement JWT later)
# jwt.secret=your_super_secret_key_here
# jwt.expiration=86400000

# Session timeout (if using session-based auth)
# server.servlet.session.timeout=30m

# Enable debug logs for Spring Security (for development)
# logging.level.org.springframework.security=DEBUG